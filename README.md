# SemanaOmnistack-BeTheHero
Um App com Express no backend, React no frontend e React-native no mobile.

## Instalação

Entre na pasta backend e frontend e use o comando npm install, para instalar todas as dependências.